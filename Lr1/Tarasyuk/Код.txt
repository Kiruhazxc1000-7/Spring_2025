J = input("Введите строку J (драгоценности): ")
S = input("Введите строку S (камни): ")

# Счётчик драгоценностей
c = 0

# Проход по каждому символу из строки S
for x in S:
    if x in J:
        c += 1

# Вывод результата
print("Количество драгоценностей в S:", c)